<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SSH Консоль"
        Height="760"
        Width="1100"
        MinHeight="560"
        MinWidth="820">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="6" />
            <RowDefinition Height="220" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="170" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="170" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" Text="IP-адрес" />
            <TextBox x:Name="HostTextBox" Grid.Column="1" Text="127.0.0.1" Margin="0,0,12,0" VerticalContentAlignment="Center" />

            <TextBlock Grid.Column="2" VerticalAlignment="Center" Margin="0,0,8,0" Text="Порт" />
            <TextBox x:Name="PortTextBox" Grid.Column="3" Text="22" Margin="0,0,12,0" VerticalContentAlignment="Center" />

            <TextBlock Grid.Column="4" VerticalAlignment="Center" Margin="0,0,8,0" Text="Пользователь" />
            <TextBox x:Name="UsernameTextBox" Grid.Column="5" Margin="0,0,12,0" VerticalContentAlignment="Center" />

            <TextBlock Grid.Column="6" VerticalAlignment="Center" Margin="0,0,8,0" Text="Пароль" />
            <PasswordBox x:Name="PasswordBox" Grid.Column="7" Margin="0,0,12,0" VerticalContentAlignment="Center" />

            <Button x:Name="ConnectButton"
                    Grid.Column="8"
                    Margin="0,0,8,0"
                    Padding="14,6"
                    Content="Подключиться"
                    Click="ConnectButton_Click" />
            <Button x:Name="DisconnectButton"
                    Grid.Column="9"
                    Padding="14,6"
                    Content="Отключиться"
                    Click="DisconnectButton_Click" />

            <TextBlock x:Name="StatusTextBlock"
                       Grid.Column="10"
                       Margin="14,0,0,0"
                       VerticalAlignment="Center"
                       FontWeight="SemiBold"
                       Text="Отключено" />
        </Grid>

        <TextBox x:Name="TerminalTextBox"
                 Grid.Row="1"
                 AcceptsReturn="True"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="13"
                 Padding="10"
                 KeyDown="TerminalTextBox_KeyDown" />

        <GridSplitter Grid.Row="2"
                      Height="6"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Center"
                      ResizeDirection="Rows"
                      ResizeBehavior="PreviousAndNext"
                      ShowsPreview="True" />

        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Margin="0,0,0,6"
                       FontWeight="SemiBold"
                       Text="Журнал" />

            <TextBox x:Name="JournalTextBox"
                     Grid.Row="1"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     FontFamily="Consolas"
                     FontSize="12"
                     Padding="10" />
        </Grid>
    </Grid>
</Window>
