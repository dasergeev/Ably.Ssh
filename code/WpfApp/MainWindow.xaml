<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        mc:Ignorable="d"
        Title="SSH Консоль"
        Height="760"
        Width="1100"
        MinHeight="640"
        MinWidth="960"
        ui:WindowHelper.UseModernWindowStyle="True">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="230" />
        </Grid.RowDefinitions>

        <ui:CommandBar Grid.Row="0"
                       DefaultLabelPosition="Right"
                       Margin="0,0,0,10">
            <ui:AppBarButton Label="Подключиться"
                             Click="ConnectButton_Click" />
            <ui:AppBarButton Label="Отключиться"
                             Click="DisconnectButton_Click" />
            <ui:AppBarSeparator />
            <ui:AppBarButton Label="Очистить терминал"
                             Click="ClearTerminalButton_Click" />
            <ui:AppBarButton Label="Очистить журнал"
                             Click="ClearJournalButton_Click" />
            <ui:CommandBar.Content>
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <TextBlock Margin="0,0,10,0"
                               VerticalAlignment="Center"
                               FontWeight="SemiBold"
                               Text="{Binding ElementName=StatusTextBlock, Path=Text}" />
                    <CheckBox x:Name="ThemeToggleSwitch"
                              VerticalAlignment="Center"
                              Content="Светлая тема"
                              Checked="ThemeToggleSwitch_Checked"
                              Unchecked="ThemeToggleSwitch_Unchecked" />
                </StackPanel>
            </ui:CommandBar.Content>
        </ui:CommandBar>

        <Border Grid.Row="1"
                Padding="12"
                Margin="0,0,0,10"
                BorderThickness="1"
                CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="170" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" Text="IP-адрес" />
                <TextBox x:Name="HostTextBox" Grid.Column="1" Text="127.0.0.1" Margin="0,0,14,0" VerticalContentAlignment="Center" />

                <TextBlock Grid.Column="2" VerticalAlignment="Center" Margin="0,0,8,0" Text="Порт" />
                <TextBox x:Name="PortTextBox" Grid.Column="3" Text="22" Margin="0,0,14,0" VerticalContentAlignment="Center" />

                <TextBlock Grid.Column="4" VerticalAlignment="Center" Margin="0,0,8,0" Text="Пользователь" />
                <TextBox x:Name="UsernameTextBox" Grid.Column="5" Margin="0,0,14,0" VerticalContentAlignment="Center" />

                <TextBlock Grid.Column="6" VerticalAlignment="Center" Margin="0,0,8,0" Text="Пароль" />
                <PasswordBox x:Name="PasswordBox" Grid.Column="7" Margin="0,0,14,0" VerticalContentAlignment="Center" />

                <TextBlock x:Name="StatusTextBlock"
                           Grid.Column="8"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Text="Отключено" />
            </Grid>
        </Border>

        <Border Grid.Row="2"
                BorderThickness="1"
                CornerRadius="8"
                Padding="8"
                Margin="0,0,0,10">
            <TextBox x:Name="TerminalOutputTextBox"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Cascadia Mono,Consolas"
                     FontSize="13"
                     Padding="10" />
        </Border>

        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="CommandTextBox"
                     Grid.Column="0"
                     Margin="0,0,8,0"
                     VerticalContentAlignment="Center"
                     FontFamily="Cascadia Mono,Consolas"
                     FontSize="13"
                     KeyDown="CommandTextBox_KeyDown" />
            <Button x:Name="SendCommandButton"
                    Grid.Column="1"
                    Padding="16,6"
                    Content="Отправить"
                    Click="SendCommandButton_Click" />
        </Grid>

        <Border Grid.Row="4"
                BorderThickness="1"
                CornerRadius="8"
                Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Margin="0,0,0,6"
                           FontWeight="SemiBold"
                           Text="Журнал" />

                <TextBox x:Name="JournalTextBox"
                         Grid.Row="1"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         FontFamily="Cascadia Mono,Consolas"
                         FontSize="12"
                         BorderThickness="0"
                         Padding="10" />
            </Grid>
        </Border>
    </Grid>
</Window>
